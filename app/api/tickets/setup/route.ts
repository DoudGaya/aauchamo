import { requireAccess, requirePermission } from "@/lib/server/access";
import { apiFailure, apiSuccess, requestIdFrom } from "@/lib/server/api";
import { db } from "@/lib/server/db";

export async function GET(request: Request) { const requestId = requestIdFrom(request); try { const access = requirePermission(await requireAccess(), "tickets.manage"); const stationIds = access.companyWide ? undefined : [...access.stationIds]; const [customers, agents, paymentMethods, businessUnits] = await Promise.all([db.customer.findMany({ where: { companyId: access.companyId, status: "ACTIVE", ...(stationIds ? { homeStationId: { in: stationIds } } : {}) }, select: { id: true, customerNumber: true, displayName: true }, orderBy: { displayName: "asc" }, take: 500 }), db.agent.findMany({ where: { companyId: access.companyId, status: "ACTIVE", ...(stationIds ? { homeStationId: { in: stationIds } } : {}) }, select: { id: true, agentNumber: true, name: true }, orderBy: { name: "asc" } }), db.paymentMethod.findMany({ where: { companyId: access.companyId, isActive: true }, select: { id: true, name: true, type: true, requiresReference: true }, orderBy: { displayOrder: "asc" } }), db.businessUnit.findMany({ where: { companyId: access.companyId, isActive: true, ...(access.companyWide || !access.businessUnitIds.size ? {} : { id: { in: [...access.businessUnitIds] } }) }, select: { id: true, code: true, name: true }, orderBy: { name: "asc" } })]); return apiSuccess({ customers, agents, paymentMethods, businessUnits }, requestId); } catch (error) { return apiFailure(error, requestId); } }
